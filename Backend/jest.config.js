export default {
  testEnvironment: "node",
  clearMocks: true,
  detectOpenHandles: true, // Helps detect unclosed async operations
  forceExit: true, // Forces Jest to exit after running tests
};